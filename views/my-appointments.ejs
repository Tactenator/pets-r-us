<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/header.ejs') %>
    <%- include('./partials/nav.ejs') %>
    <section id="registration">
        <h1 class="center-text">New customer? Welcome!</h1>
    
        <div class="card center-div">
            <div class="card-title"></div>
            <div class="card-content">
                <div class="form">
                    <form action="/customers" method="post">
                        <div class="form-field">
                            <label for="customerID">Email: </label><br />
                            <input class="input" type="text" name="email" id="email" required autofocus>
                        </div>
                        <div class="form-field">
                            <input type="submit" value="Search" class="btn btn-primary full-width">
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <table class="table">
            <tbody id="search-results">

            </tbody>
        </table>
    </section>
    
    <%- include('./partials/footer.ejs') %>
</html>
<script>
    const form = document.querySelector('form')
    let newData = handleData() 

    async function handleData() {
        const response = await fetch('https://api.npoint.io/3cd3f30244854f7ed85d'); 
        const data = await response.json(); 
        return data; 
    }


    form.addEventListener('submit', e => {
        e.preventDefault()

        async function handleSubmit() {
            const results = document.getElementById('search-results')
            const table = document.querySelector('.table')
            let userInput = document.getElementById('email').innerHTML; 
            const header = `<h1 style="text-align: center; width=100%; margin-bottom:5vh;">My Appointments</h1>`

            results.innerHTML += header; 

            const tableHeader = `<tr><th>First Name</th><th>Last Name</th><th>Email</th><th>Service</th></tr>`
            results.innerHTML += tableHeader; 

            console.log(newData)
            
        }

        handleSubmit()

    })

</script>
